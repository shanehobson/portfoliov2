# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Full-size image originals, re-encoded into public/images by
# scripts/optimize-images.sh
images-src

# Hosted media — lives in the S3 media bucket, not the repo.
# `npm run media:pull` fetches a local copy for dev; `npm run media:push`
# publishes changes. See cdk/README.md.
/public/images
/public/video

# Generated blog source pages (scripts/build-blog.mjs). content/ is the source
# of truth and is committed; everything below is build output.
/blog
/public/blog
/public/sitemap.xml
/public/robots.txt

# CDK
cdk/config.local.ts
cdk/cdk.out
cdk/node_modules
cdk/*.js
cdk/*.d.ts
cdk/.cdk.staging

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
